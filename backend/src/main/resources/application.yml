server:
  port: 8080
  forward-headers-strategy: framework
spring:
  application:
    name: backend
  session:
    store-type: none
  datasource:
    password: ${DB_PASSWORD}
    url: jdbc:postgresql://postgres:5432/seller
    username: paulzverev
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true