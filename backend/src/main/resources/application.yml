server:
  port: 8080
  forward-headers-strategy: framework
spring:
  application:
    name: backend
  session:
    store-type: none
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/seller # postgres in production
    username: paulzverev
  jpa:
    hibernate:
      ddl-auto: create-drop # update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true